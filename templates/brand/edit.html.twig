{% extends 'base.html.twig' %}
{% trans_default_domain 'crud' %}

{% block title %}{{ 'global.edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'global.edit'|trans }}</h1>

    {{ include('brand/_form.html.twig', {'button_label': ('global.update')|trans }) }}

    <a href="{{ path('brand_index') }}" class="btn btn-warning">{{ 'global.back_to_list'|trans }}</a>

    {{ include('brand/_delete_form.html.twig') }}
{% endblock %}
